[package]
name = "modern-app"
version = "0.1.0"
edition = "2021"
rust-version = "1.84"
description = "模拟新组件：MSRV 1.84，使用 clap 4 与 indexmap 2"
license = "MIT OR Apache-2.0"

[dependencies]
clap = { version = "4", features = ["derive"] }
indexmap = "2"
legacy-lib = { path = "../legacy-lib" }

# 本 crate 属于 examples/resolver_v3_practice workspace，不在此处声明 [workspace]。
