[package]
name = "build_script_practice"
version = "0.1.0"
edition = "2024"
rust-version = "1.97.0"

[dependencies]

[build-dependencies]
cc = "1"
serde_json = "1.0"

# 本示例为独立包，不属于根 workspace，避免与 workspace 的依赖解析冲突。
[workspace]
