/* 通用 RISC-V 32 位链接脚本占位符
 * 真实硬件上应根据具体芯片替换为实际 RAM/ROM 大小与起始地址。
 * 此处取值适用于常见 GD32VF103/ESP32-C3 类小容量 RISC-V 微控制器。
 */
MEMORY
{
    /* 闪存/ROM：64 KiB 起始于 0x2000_0000（XIP 模式） */
    FLASH : ORIGIN = 0x20000000, LENGTH = 64K
    /* SRAM：32 KiB 起始于 0x8000_0000 */
    RAM : ORIGIN = 0x80000000, LENGTH = 32K
}

REGION_ALIAS("REGION_TEXT", FLASH);
REGION_ALIAS("REGION_RODATA", FLASH);
REGION_ALIAS("REGION_DATA", RAM);
REGION_ALIAS("REGION_BSS", RAM);
REGION_ALIAS("REGION_HEAP", RAM);
REGION_ALIAS("REGION_STACK", RAM);
